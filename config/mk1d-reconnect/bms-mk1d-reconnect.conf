# Create the root-owned socket directory and transaction lock before systemd
# socket activation. The API receives group access only to the socket itself.
d /run/biomodstack 0755 root root -
f /run/biomodstack/mk1d-reconnect.lock 0640 root root -
