# BC2 lane E native boundary — incomplete integration

Pinned upstream: `PacesaLab/BindCraft2@d5bae16e9fee95f4c97fc16bc05dcbde4ccb885f` (1.0.1). `platform/api/config/models/bindcraft2_native_inventory.json` is generated by `services/bindcraft2_inventory.py` from the pinned checkout. Regenerate with `python platform/api/services/bindcraft2_inventory.py <pinned-checkout> > platform/api/config/models/bindcraft2_native_inventory.json`. The reference has 235 fields, but its nulls are **not** types or defaults; native `default.json` has 54 top-level entries. The static register deliberately reports unresolved reference/static differences and preset-only nested fields. It inventories statically registered loss/filter signatures and nested entry keys, but expression-only parameter defaults, runtime-composed metric entries and dynamic preset behavior still need qualification; it does not establish complete typed UI/API coverage. It must not be registered as an enabled model schema.

`services/bindcraft2_native.py` performs only a bounded execution-boundary compile. The CLI now validates the model-owned typed request before invoking the *installed pinned* `bindcraft.settings.load_settings` inside the execution environment; API discovery never imports the accelerator module. It refuses missing/nonpositive trajectory limits, caller-controlled project paths and unqualified resume. Native `parameter_sweep_arms` now determines the exact arm count after preset resolution; the compiler records per-arm and aggregate allowances and refuses native's minimum-one-arm clamp whenever it exceeds the requested aggregate. The native scheduler remains responsible for assigning actual claims (early accepted-design exit can yield fewer). `services/bindcraft2_campaign.py` stages requested and effective settings separately, refuses native scientific environment overrides, and calls the shipped native CLI. It is **not** an executable BMS route until the full typed schema, preset/nested registry audit, preview binding, installed image/assets, Nextflow/bridge and result route exist. No real native GPU campaign has been run here.

The pinned static register currently contains **239** native-known top-level fields: **130** source-observed typed and **109** unresolved with no source-backed non-null type observation (including internal placement/path fields). The 235-field reference is not the authority for assigning types. The typed form renders scalar/selected nested fields and sweeps, but not every 130 typed setting has a qualified control: arrays beyond targets/lengths/conformations/preset selectors, nested `binder_shapes`, registry parameters with expression-only defaults, and qualified prediction-state selectors still need typed contracts and mounted coverage. The 109 unresolved entries are listed exactly in `unresolved_fields` of the generated inventory; none may be quietly accepted or defaulted. Runtime scientific settings and UI/API coverage are therefore **incomplete**, not a ready model.

## Proposed producer-to-F contract (not yet installed)

Apply `apptainer/bindcraft2-producer.patch` to the exact pinned upstream source during immutable image build. Patch applies cleanly via `git apply --check` on that checkout. No scientific rank/filter/acceptance logic changes:

- Each **claimed, deduplicated recipe** gets `1_Trajectories/!_BMS_Attempts/<design>.json`, with `schema_version=1`, `design` (native trajectory stem), `trajectory` (native claim number), `recipe_hash` (native, incomplete science identity), complete `effective_settings` **after autotune and desperation**, and sampled `drawn`. Canonical JSON excludes trailing newline when calculating SHA-256; sidecar ends in newline. A claim discarded as duplicate has no sidecar. An interrupted run may have a sidecar without a trajectory row. F must not use sidecar count as claimed count. The native campaign progress store remains accounting authority.
- Native trajectory CSV adds `bms_attempt_sha256`. F joins the row's exact `design` to the sidecar filename, validates the canonical sidecar digest against the column, and compares recipe hash and claim number. `effective_settings` and `drawn` recursively encode native nonfinite float sentinels as `{"$bc2_nonfinite_float":"Infinity"}` (or `-Infinity`/`NaN`), matching the compilation receipt instead of writing nonstandard JSON or crashing on native unrejecting thresholds. A trajectory-only or zero-yield run can have no scored/retained rows.
- For each retained sequence, accepted CSV and native mmCIF structure metadata both add `bms_scored_candidate` = `ValidatedBinder.candidate_number` (1-based scored draw) and `bms_attempt_sha256`. Join its `<trajectory>_seqN` retained identity to `<trajectory>_candidate<bms_scored_candidate>` in `2_Refolded` CSV; **never** infer this association from `seqN` order. Target-state CIF documents with the same retained identity and different state suffixes remain separate documents. Native rank may reorder further; use exact design identity rather than row position.
- The scored candidate CSV itself is unchanged. F must verify join cardinality, missing artifacts, and contradictions explicitly; a failed hook or absent sidecar is an incomplete provenance state, not license for ordinal reconstruction. BMS must persist an immutable full request digest independently from native recipe hash; same-folder resume is disabled until immutable identity and complete native state are qualified. Do not return only ranked rows: native hidden counters, sweep state, trajectory/refold/accepted/failed artifacts and sidecars belong to the return closure.

## Integrator handoff / blockers

- **Parent (shared sole writer):** do not register `bindcraft2` as enabled or advertise this inventory as a full schema. Once all unresolved native fields, nested controls and differential preflight are covered, wire typed discovery, preview digest and immutable submit/save/clone/retry admission into `jobs.py`/`model_registry.py`; register `MODEL_MODE_WORKFLOW_ENTRYPOINTS` in `services/nextflow.py` and its parameters in `nextflow_schema.json` only after a compiled runnable Nextflow route exists. Ensure job-detail effective and attempt-settings readback through C's owner. Current CLI accepts typed input but is not server request authority.
- **G (runtime closure):** build and qualify immutable cuda13 image from the exact upstream commit with this producer patch, locked dependency resolution, BMS compiler source, shipped MPNN plus shared AF2 params (`BINDCRAFT_AF2_PARAMS`), durable JAX compilation cache, scheduler-isolated visible cards and selected portable target/scaffold inputs. No first-campaign downloads; return the full native campaign tree, including hidden progress/sweep/sidecar state, through the existing bridge. This slice has **no** built image or Nextflow entrypoint; no local/remote dispatch proof.
- **F (native publication):** parse sidecar canonical hashes and nonfinite sentinel values; join trajectory by exact `design`, accepted `_seqN` to scored `_candidateN` only by emitted `bms_scored_candidate`, and verify each CIF's hook metadata. Preserve distinct arm/attempt/scored/retained/state identities, zero-yield, native failures, and settings identity in model-owned datasets; hand immutable artifacts to C rather than inventing filenames or Designs. A sidecar from a claimed but interrupted attempt is not itself a trajectory.

**Unresolved P2 gate:** type and control the 109 unresolved inventory fields as relevant or prove a system-only classification per field; finish all nested registry/preset/action controls and appropriate typed selectors; execute a real pinned GPU campaign and verify full native artifact/return/selection path. The CPU settings differential and unit fixtures below do not satisfy that gate.
