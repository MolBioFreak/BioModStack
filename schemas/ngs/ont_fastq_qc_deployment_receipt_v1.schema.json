{
  "$id": "https://biomodstack.local/schemas/ngs/ont_fastq_qc_deployment_receipt_v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "api_process": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "candidate_commit": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "candidate_tree": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "canonical_checkout_commit": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "canonical_checkout_tree": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "database": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "deployed_at": {
      "format": "date-time",
      "pattern": "Z$",
      "type": "string"
    },
    "fence": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "frontend_build": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "lane": {
      "const": "development"
    },
    "listeners": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "migrations": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "quiescence": {
      "additionalProperties": false,
      "properties": {
        "canonical_preimage_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "receipt_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "schema": {
          "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
          "type": "string"
        },
        "source": {
          "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "source",
        "canonical_preimage_sha256",
        "receipt_sha256"
      ],
      "type": "object"
    },
    "receipt_sha256": {
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    },
    "release_backups": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "canonical_preimage_sha256": {
            "pattern": "^[0-9a-f]{64}$",
            "type": "string"
          },
          "receipt_sha256": {
            "pattern": "^[0-9a-f]{64}$",
            "type": "string"
          },
          "schema": {
            "pattern": "^bms\\.[a-z0-9.-]+\\.v1$",
            "type": "string"
          },
          "source": {
            "pattern": "^[A-Za-z0-9_.:@/-]{1,255}$",
            "type": "string"
          }
        },
        "required": [
          "schema",
          "source",
          "canonical_preimage_sha256",
          "receipt_sha256"
        ],
        "type": "object"
      },
      "maxItems": 3,
      "minItems": 1,
      "type": "array"
    },
    "remote_test_commit": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "remote_test_tree": {
      "pattern": "^[0-9a-f]{40}$",
      "type": "string"
    },
    "schema": {
      "const": "bms.ont-fastq-qc-deployment-receipt.v1"
    }
  },
  "required": [
    "schema",
    "lane",
    "candidate_commit",
    "candidate_tree",
    "remote_test_commit",
    "remote_test_tree",
    "canonical_checkout_commit",
    "canonical_checkout_tree",
    "fence",
    "quiescence",
    "release_backups",
    "frontend_build",
    "api_process",
    "listeners",
    "database",
    "migrations",
    "deployed_at",
    "receipt_sha256"
  ],
  "type": "object"
}
