{
  "$id": "bms.evidence-requirement.v2",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "oneOf": [
    {
      "additionalProperties": false,
      "properties": {
        "payload": {
          "$ref": "bms.evidence-requirement.native-receipt.v1"
        },
        "required": {
          "type": "boolean"
        },
        "requirement_id": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "schema_id": {
          "const": "bms.evidence-requirement.native-receipt.v1"
        },
        "schema_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "subject_role": {
          "enum": [
            "input",
            "sample",
            "reference",
            "target",
            "panel",
            "control",
            "result",
            "comparison",
            "evidence",
            "other"
          ]
        }
      },
      "required": [
        "requirement_id",
        "schema_id",
        "schema_sha256",
        "subject_role",
        "payload",
        "required"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "properties": {
        "payload": {
          "$ref": "bms.evidence-requirement.operator-observation.v1"
        },
        "required": {
          "type": "boolean"
        },
        "requirement_id": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "schema_id": {
          "const": "bms.evidence-requirement.operator-observation.v1"
        },
        "schema_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "subject_role": {
          "enum": [
            "input",
            "sample",
            "reference",
            "target",
            "panel",
            "control",
            "result",
            "comparison",
            "evidence",
            "other"
          ]
        }
      },
      "required": [
        "requirement_id",
        "schema_id",
        "schema_sha256",
        "subject_role",
        "payload",
        "required"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "properties": {
        "payload": {
          "$ref": "bms.evidence-requirement.result-artifact.v1"
        },
        "required": {
          "type": "boolean"
        },
        "requirement_id": {
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "schema_id": {
          "const": "bms.evidence-requirement.result-artifact.v1"
        },
        "schema_sha256": {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        "subject_role": {
          "enum": [
            "input",
            "sample",
            "reference",
            "target",
            "panel",
            "control",
            "result",
            "comparison",
            "evidence",
            "other"
          ]
        }
      },
      "required": [
        "requirement_id",
        "schema_id",
        "schema_sha256",
        "subject_role",
        "payload",
        "required"
      ],
      "type": "object"
    }
  ]
}
